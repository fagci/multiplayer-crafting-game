define(function (require) {
    "use strict";
    var THREE  = require('three'),
        camera = require('camera');

    var player_body_geometry = new THREE.CubeGeometry(0.5, 2, 0.25, 1, 1, 1),
        player_mateerial;
});